# Copie este arquivo para .env e preencha com suas chaves reais
# cp .env.example .env

# Chave principal (deepseek-v4-flash com reasoning)
NVIDIA_API_KEY_1=sua_chave_principal_aqui

# Chave de fallback (usada automaticamente se a principal der 429)
NVIDIA_API_KEY_2=sua_chave_fallback_aqui
